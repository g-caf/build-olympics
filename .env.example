# Production Environment Variables for Render Deployment

# Admin key for accessing signup data (REQUIRED - use strong, unique key)
ADMIN_KEY=your_secret_admin_key_2025_secure

# Competitor admin passcode for competitor dashboard access (REQUIRED - use strong, unique passcode)
COMPETITOR_ADMIN_PASSCODE=your_competitor_admin_passcode_2025_secure

# Node environment
NODE_ENV=production

# Port (Render sets this automatically, but default for local dev)
PORT=10000

# Email configuration for signup notifications (REQUIRED for email notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password_16_chars
NOTIFY_EMAIL=admin@yourdomain.com

# Database configuration
# SQLite is used by default, but you can specify PostgreSQL for production
# DATABASE_URL=postgresql://user:password@host:port/database

# Security settings (optional)
# CORS_ORIGIN=https://yourdomain.com
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
